import { everyNth } from 'super-articles/helpers/every-nth';
import { module, test } from 'qunit';

module('Unit | Helper | every nth');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = everyNth([42]);
  assert.ok(result);
});
